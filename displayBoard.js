const createBoard = require('./createBoard')
board = createBoard(5)
board[0][1] = 3
function displayBoard (board) {
  //-----------------------------------------------
  // eslint-disable-next-line no-console
  //-----------------------------------------------
}
